<% if params[:query] == "1" %>
  <% @products.each do |product, total_sales| %>
  <tr class="card-dashboard-effects">
    <td class="w-25 bg-white">
      <div class = "d-flex">
        <%= image_tag "https://source.unsplash.com/random/?#{product.category}", class: "img-product-info" %>
        <ul class="list-unstyled m-0 text-left ml-3">
          <li class="mt-1"><strong class="name-column"><%= product.name %></strong></li>
          <li class="text-muted">Codigo: <%= product.bar_code %></li>
        </ul>
      </div>
    </td>
    <td class="column-info bg-white"><strong style="color: #013440;"><%= product.category %></strong></td>
    <td class="column-info bg-white font-weight-bold">S/ <%= product.price %></td>
    <td class="column-info bg-white font-weight-bold <%= "stock-alert" if product.stock < product.minimum_required %>"><%= product.stock %></td>
    <td scope="row" class="column-info font-weight-bold"> <%= product.size %> </td>
    <td class="column-info bg-white font-weight-bold"><%= total_sales %></td>
    <td class="column-info bg-white font-weight-bold"><%=  %></td>
    <td class="column-info bg-white font-weight-bold"><%=   %></td>
  </tr>
<% end %>

<% elsif (params[:query].present? && params[:query] == "2") || params[:query].nil? %>
  <% @products.each do |item| %>
    <tr class="card-dashboard-effects">
      <td class="w-25 bg-white">
        <div class = "d-flex">
          <%= image_tag "https://source.unsplash.com/random/?#{item.product.category}", class: "img-product-info" %>
          <ul class="list-unstyled m-0 text-left ml-3">
            <li class="mt-1"><strong class="name-column"><%= item.product.name %></strong></li>
            <li class="text-muted">Codigo: <%= item.product.bar_code %></li>
          </ul>
        </div>
      </td>
      <td class="column-info bg-white"><strong style="color: #013440;"><%= item.product.category %></strong></td>
      <td class="column-info bg-white font-weight-bold">S/ <%= item.product.price %></td>
      <td class="column-info bg-white font-weight-bold <%= "stock-alert" if item.product.stock < item.product.minimum_required %>"><%= item.product.stock %></td>
      <td scope="row" class="column-info font-weight-bold"> <%= item.product.size %> </td>
      <td class="column-info bg-white font-weight-bold"><%= item.quantity %></td>
      <td class="column-info bg-white font-weight-bold"><%= item.order.customer.name %></td>
      <td class="column-info bg-white font-weight-bold"><%= item.created_at&.strftime("%d/%m/%Y")  %></td>
    </tr>
  <% end %>

<% elsif params[:query] == "3" %>
  <% @products.each do |item| %>
    <tr class="card-dashboard-effects">
      <td class="w-25 bg-white">
        <div class = "d-flex">
          <%= image_tag "https://source.unsplash.com/random/?#{item.product.category}", class: "img-product-info" %>
          <ul class="list-unstyled m-0 text-left ml-3">
            <li class="mt-1"><strong class="name-column"><%= item.product.name %></strong></li>
            <li class="text-muted">Codigo: <%= item.product.bar_code %></li>
          </ul>
        </div>
      </td>
      <td class="column-info bg-white"><strong style="color: #013440;"><%= item.product.category %></strong></td>
      <td class="column-info bg-white font-weight-bold">S/ <%= item.product.cost %></td>
      <td class="column-info bg-white font-weight-bold <%= "stock-alert" if item.product.stock < item.product.minimum_required %>"><%= item.product.stock %></td>
      <td scope="row" class="column-info font-weight-bold"> <%= item.product.size %> </td>
      <td class="column-info bg-white font-weight-bold"><%= item.quantity %></td>
      <td class="column-info bg-white font-weight-bold"><%= item.purchase.supplier.name %></td>
      <td class="column-info bg-white font-weight-bold"><%= item.created_at&.strftime("%d/%m/%Y")  %></td>
    </tr>
  <% end %>

<% end %>
