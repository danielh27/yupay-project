<%= simple_form_for(list_purchase) do |f| %>
  <%= f.association :product, label: "Selecciona un producto", prompt: "Producto....." %>
  <%= f.input :quantity, label: "Cantidad" %>
  <%= f.submit %>
<% end %>
