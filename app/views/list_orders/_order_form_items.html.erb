<%= simple_form_for(list_order, url: order_list_orders_path,
                    data: { insert_in_list_target: 'form',
                    action: 'submit->insert-in-list#send' }) do |f| %>

  <%= f.association :product, label: "Selecciona un producto", prompt: "Producto....." %>
  <%= f.input :quantity, collection: 1..2 , label: "Cantidad", data: { insert_in_list_target: "dropdown", action: "change->insert-in-list#update_select_options" } %>
  <%= f.submit "Añadir Item", class: "btn-primary" %>
<% end %>
