<%= simple_form_for(@list_order, url: order_list_orders_path,
          data: { action: 'submit->insert-in-list#send' }, html: { class: "" }) do |f| %>

  <%= f.hidden_field :product_id, value: product.id.to_i %>
  <%= f.input :quantity, label: false, collection: 1..product.stock, prompt: "Cantidad", error: "Selecciona la cantidad"%>
  <%= f.submit "Añadido", class: "btn btn-actions w-75 ml-2 disabled btn-actions-active" %>
<% end %>
