
<div class="container">
  <p> <%= @product.name %> </p>
  <p> Categoria: <%= @product.category %> </p>
  <p> Talla: <%= @product.size %> </p>
  <h2> S/. <%= @product.price %> </h2>
  <p> <%= @product.cost %> </p>
  <p> Stock: <%= @product.stock %> </p>
  <p> <%= @product.minimum_required %> </p>

  <%# boton borrar %>
   <%= link_to "Borrar producto", product_path(@product), :method => :delete, data: { confirm: 'Are you sure you want to delete?' }  %>

<%# link_to "Delete Book", @book, method: "delete", { confirm: "Are you sure?", disable_with: "Processing..." } %>

</div>